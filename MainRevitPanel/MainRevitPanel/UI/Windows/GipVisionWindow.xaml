<Window x:Class="MainRevitPanel.UI.Windows.GipVisionWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainRevitPanel.UI.Windows"
             mc:Ignorable="d" 
             Title="GIP VISION" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
           FontSize="14" 
           FontWeight="Bold" 
           Margin="0,0,0,10"/>

        <DataGrid ItemsSource="{Binding Items}"
        SelectedItem="{Binding SelectedItem}"
        Grid.Row="1" 
        AutoGenerateColumns="False" 
        IsReadOnly="True"
        SelectionMode="Single"
        Margin="0,0,0,10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Проект" Binding="{Binding Name}" Width="230"/>
                <DataGridTextColumn Header="URL" Binding="{Binding URL}" Width="320"/>
                <DataGridTextColumn Header="Pin-Code" Binding="{Binding PIN_CODE}" Width="80"/>
                <DataGridTextColumn Header="Действителен до" Binding="{Binding Data}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" 
            Orientation="Horizontal" 
            HorizontalAlignment="Center"
            Margin="0,0,0,10">

            <Button Content="Копировать URL" 
                Width="130" 
                Margin="5"
                Command="{Binding CopyUrlCommand}"/>

            <Button Content="Копировать Pin-Code" 
                Width="150" 
                Margin="5"
                Command="{Binding CopyPinCommand}"/>

            <Button Content="Закрыть" 
            Width="100" 
            Margin="5"
            Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</Window>
