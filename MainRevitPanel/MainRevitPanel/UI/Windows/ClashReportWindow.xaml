<Window x:Class="MainRevitPanel.UI.Windows.ClashReportWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:MainRevitPanel.UI.Windows"
            mc:Ignorable="d" 
            Title="Отчет о пересечениях" 
            Height="500" 
            Width="800"
            WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   x:Name="SummaryText" 
                   FontSize="14" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <DataGrid x:Name="ClashesGrid" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="0,0,0,10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Воздуховод" Binding="{Binding DuctName}" Width="200"/>
                <DataGridTextColumn Header="ID возд." Binding="{Binding DuctId}" Width="80"/>
                <DataGridTextColumn Header="Труба" Binding="{Binding PipeName}" Width="200"/>
                <DataGridTextColumn Header="ID трубы" Binding="{Binding PipeId}" Width="80"/>
                <DataGridTextColumn Header="Объем (см³)" Binding="{Binding Volume, StringFormat={}{0:F2}}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,0,0,10">

            <Button x:Name="HighlightButton" 
                    Content="Подсветить выбранное" 
                    Click="HighlightButton_Click"
                    Width="150" 
                    Margin="5"/>

            <Button x:Name="ExportButton" 
                    Content="Экспорт в JSON" 
                    Click="ExportButton_Click"
                    Width="150" 
                    Margin="5"/>

            <Button x:Name="CloseButton" 
                    Content="Закрыть" 
                    Click="CloseButton_Click"
                    Width="100" 
                    Margin="5"/>
        </StackPanel>

        <TextBlock x:Name="StatusBar" 
                   Grid.Row="3" 
                   Text="Готово" 
                   FontStyle="Italic"
                   Foreground="Gray"/>
    </Grid>
</Window>
